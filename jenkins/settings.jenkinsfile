import groovy.transform.Field

// This is used just as a check to make sure that this file was considered when creating a new branch
@Field String branchName = 'release/1.2.2'

@Field String slackChannel = '#operations1'
@Field String jobDescription = 'release of new version'

return this;
